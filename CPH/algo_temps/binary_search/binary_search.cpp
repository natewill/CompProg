using namespace std;

#define F first
#define S second
#define PB push_back
#define MP make_pair

typedef long long ll;
int main(){
  //make i/o faster
  ios::sync_with_stdio(0);
  cin.tie(0);

  int a = 0; b = n-1;

  while(a <= b) {
    int k = (a+b)/2; 
    if(array[k] = x){
      // x found at index k
    }
    if(array[k] > x)
      b = k-1;
    else a = k+1;
  }
}

